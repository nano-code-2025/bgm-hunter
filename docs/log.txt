❯ 1.我希望用DeepSeek api实现口播稿分析。2.我希望前端有足够的审美，如果需要skill可以提出来。3.给我一个用于让manus生成完整
  方案的文档，并告诉我是否可以让KIMI 2.5也适用。最后我会下载生成的文件再次让claude code完善 4.注意接口，以及代码的规范性
  ，可拓展性，可测试性，可模块化。尽量不要硬编码。5.上述过程有任何不清楚或者需要简化的地方，可以提出来讨论。

  工具,适用对象,速度,准确度
Context7 MCP,"主流开源库（React, Stripe, AWS 等）",极快,极高（结构化数据）
Web Fetch,任意网页、博客文章、GitHub Readme,中等,高（原网页内容）
Puppeteer,动态文档、后台系统、私有云文档,较慢,最高（可见即可得）

设置 CLAUDE.md（推荐）： 在你的项目根目录下创建一个 CLAUDE.md 文件。在里面加入一条指令：

"当遇到未知的库或 API 时，优先使用 context7 检索最新文档；如果无法检索，请使用 web_fetch 访问官网文档，确保代码版本是最新的。" 这样，Claude Code 每次工作前都会自动加载这个策略。